
export const toDoReducer = (initialState = [], action ) => {
    switch (action.type) {
        case 'ABC':
            throw new Error('Action no implementada');
           
    
        default:
            return initialState
            
    }
}